# 更新履歴 (CHANGELOG)

`BumpMesh_Color` の各バージョンにおける機能追加・変更・不具合修正の記録です。

---

## ★ [v1.2.1] - 2026-09-26 (最新安定版: PrusaSlicer 3.0.0+ 対応・新ファセットアノテーション & プロジェクトJSON対応・2.9.6/Bambu/Orca 完全互換)
### Added
- **PrusaSlicer 3.0.0 以降のフルリライト仕様に完全対応**:
  - **ファセットアノテーション JSON (`Metadata/Slic3r_facets_annotation.json`) 自動生成**:
    - PrusaSlicer 3.0.0 で独立ファイル化された三角形ペイント情報（`mmSegmentationFacets` 配列）を 3MF パッケージ内に自動生成・同梱。
    - 3.0.0 系スライサーで開いた際にカラーペイント情報（マルチマテリアル割り当て）が消失して単色化する問題を根本解決。
  - **PrusaSlicer 3.0.0 プロジェクトメタデータ (`Metadata/PrusaSlicer3_project.json`) の同梱**:
    - 3.0.0 系で新たに導入されたプロジェクト定義 JSON を同梱し、`_rels/.rels` にリレーションを登録。
    - スライサーが単なる「外部生メッシュ（STL相当）」ではなく「プロジェクトモデル」として認識し、ビルドプレート上の配置座標がリセットされたり自動整列で狂ったりする問題を解消。
  - **`slic3rpe:MmPaintingVersion` メタデータの定義**:
    - `3D/3dmodel.model` 内に `<metadata name="slic3rpe:MmPaintingVersion">1</metadata>` を明記し、3.0.0 および 2.x の両パーサーでペイントデータ有効判定を保証。
  - **`[Content_Types].xml` への JSON 拡張子定義追加**:
    - 3MF パッケージ規格に準拠し `<Default Extension="json" ContentType="application/json"/>` を追加。

### Changed
- **3MF コンポーネント階層の標準化 (Components Tree)**:
  - 従来の単一オブジェクト直接配置から、3MF 公式仕様および Prusa/Bambu 規格に準拠した 3 階層構造（Mesh `id="1"` → Volume `id="2"` → Object `id="3"` → Build Item `objectid="3"`）へ刷新。
  - `<build><item ...>` にアフィン変換行列 `transform="1 0 0 0 1 0 0 0 1 0 0 0"` を明示し、座標位置の消失を防止。
- **マルチスライサー ハイブリッド互換性の維持**:
  - PrusaSlicer 2.9.6 以前向けの `Metadata/Slic3r_PE.config`（XML）および `3D/3dmodel.model` の三角形ペイント属性（`slic3rpe:mmu_segmentation`）を完全維持。
  - Bambu Studio および OrcaSlicer 向けの `Metadata/model_settings.config`（XML）および `paint_color` 属性も完全維持。
  - 同一の 3MF ファイルを **PrusaSlicer 3.0.0+**, **PrusaSlicer 2.9.6**, **Bambu Studio**, **OrcaSlicer** のいずれにドラッグ＆ドロップしても、エラーなくカラーと座標が正確に読み込まれるクロス互換性を実現。

---

## [v1.2.0] - 2026-09-22 (振り重ねグラデーション階調保持・変調画像プレビュー・UI最適化)
### Added
- **振り重ねマルチツール・グラデーションモードの階調保持改善 (フルレンジ輝度＆ガンマ補正)**:
  - **暗部クリッピングの解消**:
    - 従来の K-means 代表色ベクトル射影による「暗部（髪の毛や細かな陰影）が黒潰れ・均一平坦化してしまう問題」を抜本改良。
    - ITU-R BT.709 フルレンジ輝度（0.0〜1.0）とガンマ補正（`Math.pow(lum, gamma)`）を導入し、暗部から明部まで豊かな連続階調表現を実現。
  - **全パイプライン完全一致**:
    - 3Dビューワーのプレビューシェーダー（`previewMaterial.js`）、マルチツール 3MF エクスポート（`main.js` / `exporter.js`）、2D変調プレビューで同一の計算式を適用し、画面で見えている通りのスライス結果を保証。
  - **階調調整（ガンマ）スライダー追加**:
    - 「階調表現」がグラデーションモードの際に「階調調整 (ガンマ)」スライダー（0.40〜2.20、標準 1.00）が出現。暗部の引き締めや明暗バランスをリアルタイムに微調整可能。
- **振り重ね変調画像（2Dマップ）確認プレビューモーダル新設**:
  - スライス・エクスポート前に、実際に層ごとの厚み変調に使われる重みマップ画像を画面上で直接確認できるモーダルを新設。
  - **3つの表示モード**:
    - ① `K-means重心クリッピング`（旧方式との比較用）
    - ② `階調保持（フルレンジ輝度 / 暗部保持）`（標準採用・デフォルト）
    - ③ `元テクスチャ画像`
  - **インタラクティブインスペクター**:
    - 画像上にカーソルを乗せるだけで、ピクセル座標、元RGB、ガンマ適用後重み、生輝度、旧方式との重み比較をリアルタイム表示。
    - モーダル内でも階調ガンマスライダーを操作して即座にプレビュー更新可能。
  - **画像保存**:
    - 表示中の変調画像を直接 PNG 形式でダウンロード保存可能。
  - **完全多言語対応**:
    - モーダル内の全タイトル、表示モード名、注釈、操作ボタンを全14言語に完全ローカライズ。
- **COLOR & MULTI-TOOL パネルのUI配置最適化**:
  - **Extruder数（色数）スライダーの最上部配置・強調**:
    - 最重要パラメータである Extruder 数をパネル最上部に配置し、ハイライト枠で際立たせて迷わず操作可能に。
  - **カラーパレット一覧（各ツールの色・ツール番号割り当て）の共通化**:
    - パレットリストを「カラー量子化」と「交互積層（振り重ね）」の共通領域に配置。
    - 交互積層タブを開いたままでも各ツールの色や番号割り当ての確認・編集がすべて完結。
  - **交互積層タブのノイズテキスト削減とボタン独立**:
    - 重複していたスライス情報テキストを整理し、「🖼️ 振り重ね変調画像（2Dマップ）を確認」ボタンを太字・高コントラストで押しやすく独立配置。
  - **非テクスチャ部ツール（底面等）の控えめ配置**:
    - 自動決定される項目のためパネル最下部に移動し、控えめな点線区切りと淡いトーンに整理。
- **UIレイアウト・余白の細部調和 (Pixel Perfection)**:
  - サイドバー最上部の「自動保存: [短期保存] [長期保存]」について、左右余白（16px）を下位セクションの見出し（プリセットモデル・ディスプレイスメントマップ）と完全に垂直一直線に揃え、美しくシームレスに統合。
### Fixed
- **多言語辞書（i18n）の動的インポートにおけるキャッシュバスティング対応**:
  - `i18n.js` の言語ファイル読み込み（`_loadLang`）でバージョンクエリが付与されておらず、ブラウザキャッシュにより古い言語定義（キー未追加版）が読み込まれ、一部のボタンやスライダーラベル（`color.interleavedPreviewMap`, `color.interleavedGamma`）がキー名のまま表示されてしまう問題を修正。

---

## [v1.1.4] - 2026-09-21 (プロジェクト保存強化・自動保存モード切替・プライバシーポリシー)
### Added
- **プロジェクト保存・読込（.bumpmesh）の完全対応**:
  - 読み込んだ 3D モデル、テクスチャ画像、各種パラメータ、カラーパレット、ツール割り当てをすべて 1 ファイル（ZIP形式の `.bumpmesh`）として保存・復元可能に。
- **自動保存モード切替（短期保存 / 長期保存）**:
  - タブを閉じるまで保持する `sessionStorage`（短期保存）と、ブラウザ終了後も永続保持する `localStorage`（長期保存）の切替UIを新設。
- **プライバシーポリシー（インプリント）モーダル新設**:
  - ブラウザ内ローカル完全処理、外部通信ゼロ、Cookie完全不使用、ストレージ利用の安全性を明記。

---

## [v1.1.3] - 2026-09-20 (ツール番号の短期記憶保持)
### Added
- **カラー＆マルチツール番号設定の短期記憶（永続性）対応**:
  - **テクスチャ変更・読み込み時のツール番号維持**:
    - ユーザーがパレット内の各色に割り当てたツール番号（Tool 1〜8、例: Tool 7, Tool 8）をセッション中に短期記憶。
    - 新しいテクスチャ（プリセットやカスタム画像）を読み込んだり切り替えたりした際も、設定したツール番号がデフォルトの 1, 2... にリセットされず、ユーザーの指定したツール番号が自動復元・維持されるよう改善。
    - マッピングモード（円周マッピング等）と同様に、ユーザーの操作セッションを通じて直感的に設定が持続するよう統一。
  - **プロジェクト保存（スナップショット）連動**:
    - 設定スナップショットにも `userAssignedToolIds` を含め、プロジェクトファイルやUndo履歴でもツール番号の記憶状態が正しく保持されるよう対応。

---

## [v1.1.2] - 2026-09-20 (モデル拡大縮小機能)
### Added
- **モデル拡大縮小（スケーリング）機能の追加**:
  - **パーセント即時入力・リアルタイム反映**:
    - 3Dビューア画面左下のモデル情報（三角形数・外形寸法表示）の隣に、モデルのスケール倍率（%）コントロールを配置。
    - 実行ボタンを押すことなく、X / Y / Z のパーセント数値を入力・変更すると即座にモデルサイズへ反映。
    - 隣接するモデル実寸法（mm）や地面の寸法線もリアルタイムに連動更新。
  - **縦横比固定（均等スケーリング）ロック**:
    - デフォルトで縦横比固定（🔒）が有効。X・Y・Zのいずれかを変更するだけで全体のプロポーションを維持したまま一括で拡大縮小可能。
    - ロックを解除することで、各軸ごとの個別スケーリング（非均等拡大縮小）にも対応。
  - **100%リセット ＆ カメラフィット**:
    - 元のサイズに一発で戻せる `100%` リセットボタンを搭載。
    - 拡大縮小後にモデル全体を画面に収める `⛶`（カメラフィット）ボタン（Enterキーでも連動）を搭載。
  - **パイプライン・エクスポート完全連動**:
    - スケーリング後のジオメトリサイズをそのまま維持して STL / 3MF / マルチカラー3MF のエクスポートが可能。
    - モデルサイズの変動に合わせてテクスチャ細分割ピッチ（refineLength）も自動適正化。

---

## [v1.1.1] - 2026-09-20 (自動サイズ調整モード・プリセット拡張)
### Added
- **画像比率連動のモデル自動サイズ調整モード（隠しモード）の追加**:
  - **画像アスペクト比追従**:
    - 選択されたテクスチャ画像の縦横比から、指定したモデル高さ（デフォルト 100mm）と一周の画像枚数（デフォルト 1）に応じてモデル外周長を自動算出。
    - モデルの外周がちょうど画像 1 枚（または複数枚）で一周するように、幅・直径をリアルタイム自動決定。
  - **3D形状の動的生成（高精細メッシュ）**:
    - **n角形柱（Regular n-gon Prism）**: 辺数 n ≧ 3 を指定可能。側面の各パネルを約 0.8〜1.0mm ピッチで格子状に高密度グリッド細分化し、ディスプレイスメントや積層スライスに対応。
    - **筒（円柱 / Cylinder）**: 円周 128〜256 分割、高さ 100〜200 分割の高精細メッシュ。
    - **お椀（Bowl）**: 指定高さと外径に合わせて滑らかに立ち上がる旋盤メッシュ。
  - **テクスチャ自動フィット**:
    - 自動調整モード有効時は、画像が外周にぴったり一周フィットするよう円柱マッピング（Cylindrical Mapping）とスケール値を自動設定。
  - **隠しモード解禁ギミック**:
    - `Preset Models` ヘッダーのダブルクリック、右端の `✦` ボタン、または URL パラメータ `?mode=auto` によりモード切替タブ（[通常] / [自動調整]）を解禁。
- **通常モードプリセットの整理**:
  - キューブ（50×50×50 mm）、筒（Ø80 × 100 mm）、お椀（Ø80 × 45 mm）、アップロード（カスタムファイル選択）を通常モードとして整理。

---

## [v1.1.0] - 2026-09-19
### Added
- **マルチカラー3MFエクスポート時のカラーサムネイル自動生成機能**:
  - **オフスクリーン・高精細3Dレンダリング**:
    - エクスポート時、各ツールの配色パレット（`exportPalette`）と三角形ツール割り当て（`triTools`）を反映したカラー3Dモデルをブラウザ内で一時レンダリングし、高精細な 256x256 PNG サムネイルを自動撮影・生成。
    - 明るいオフホワイト背景（`#f4f4f6`）と最適なアイソメトリック見下ろしアングルにより、Windows エクスプローラーの白背景・黒背景のどちらでも美しく立体が際立つデザインを採用。
  - **全スライサー・全OS規格のサムネイル完全網羅（全乗せ）**:
    - **Bambu Studio / OrcaSlicer**: `/Metadata/plate_1.png`、`/Auxiliaries/.thumbnails/`、公式 Relationship（`cover-thumbnail-middle`, `cover-thumbnail-small`）を格納。
    - **PrusaSlicer**: `/Metadata/thumbnail.png`、XML 内 `<metadata name="Thumbnail">` を格納。
    - **Windows 標準（Microsoft 3MF Shell Thumbnail Handler）**: 3MF Core / OpenXML 公式規格（`_rels/.rels`）に準拠したサムネイル定義を完全網羅。ZIP の先頭にサムネイルを配置して高速ストリーミング読み込みに対応。
    - これにより、Bambu Studio や PrusaSlicer をお使いの環境はもちろん、スライサーを導入していない Windows 標準環境でも、エクスプローラー上で即座に鮮やかなカラーサムネイルが表示されるよう対応。
- **外部3Dビューワー（F3D等）との完全互換性確保**:
  - 一部ビューワーの 3MF パーサー（Assimp 5.4.0 等）でマテリアル未解決エラーを引き起こす原因となっていた非標準の属性を安全に整理し、スライサーのマルチカラー認識（`slic3rpe:mmu_segmentation` / `paint_color`）を完全維持したまま、外部ビューワーでも破損やブラックアウトを起こさずクリーンに描画されるよう最適化。

---

## [v1.0.34] - 2026-09-18
### Fixed
- **交互積層モードにおける解像度低下（急峻なテクスチャ境界や微細輪郭が粗い多角形になる問題）の完全解消**:
  - **根本原因の特定**:
    - v1.0.26 で古い再帰スライサーの OOM（メモリ不足）対策として導入されていた `Math.max(settings.refineLength, 2.5)` が残存していた。
    - これにより、ユーザーがUIスライダーで設定した細分化長（例: 0.8mm や 1.0mm）が完全に無視され、強制的に 2.5mm という極めて粗いメッシュ（1辺2.5mmの三角形）で打ち切られていた。
    - φ80の円筒（円周251mm）において 2.5mm は円周わずか100分割程度にしかならず、微細な輪郭やディテールが粗い直線的ポリゴンにガタガタに潰れてしまっていた（v1.0.25 ではこの制限が緩かったため高精細に出力されていた）。
  - **根本修正**:
    - 現在は v1.0.26 でダイレクト・ポリゴンクリッパー（`sliceMeshWatertight`）に刷新されており、メモリ爆発は発生しないため、過剰な 2.5mm クランプを撤廃。
    - ユーザーがUIで指定した `refineLength`（0.8mm〜1.0mm等）がそのまま反映されるよう修正し、極端な設定での暴走を防ぐ安全下限（0.6mm）のみを残した。
    - これにより、v1.0.25 と同等以上の滑らかで高精細なディテールと曲面が完全に復活。
- **実機STL（`Cilinder55.3.stl`：高さ方向未分割の長大三角形モデル）での動作確認**:
  - ユーザー提供の実機モデル `Cilinder55.3.stl`（Z方向100mmの長大な三角形）において、高精細細分化（`refineLength: 1.0mm`）と水密スライス・レイヤー変位が一切のエラーなく完全に完走し、v1.0.33 で実装した「最上2レイヤー変位0（Pristine Rim）」によってオープンエッジ0で正常にエクスポートできることを検証。

---

## [v1.0.33] - 2026-09-18
### Fixed
- **φ55.3等小径円筒における天井付近の階段状オープンエッジ・放射状櫛の歯インフィルの完全解消**:
  - **根本原因の特定**:
    - 円筒投影（Cylindrical Mapping）では、V方向UV座標が円周（2π r）に反比例するため、φ80に比べてφ55.3の円筒ではテクスチャが縦方向に大きく引き伸ばされ、白黒の高コントラスト境界が天井直前（Z=98〜100mm）に直撃していた。
    - 従来の境界フェード処理が「頂点のZ座標」に基づいて減衰を計算していたため、フラット段差モードであるにもかかわらず、同一レイヤー内の上下頂点で変位量がズレて側面ポリゴンが円錐台状に傾斜（テーパー化）していた。
    - この傾斜した壁と水平シェルフ三角形の間に鋭角な自己交差が生じ、PrusaSlicerがスライス断面で閉輪郭を正しく抽出できずに階段状オープンエッジ（ピンク色の線）を検出、さらに傾斜したオーバーハング壁を支えようと全周に幅1〜2mmの放射状ジグザグインフィル（櫛の歯）を誤充填していた。
  - **根本修正**:
    1. **レイヤー単位の離散フェード（`getLayerFade`）の導入**:
        - フェード処理を頂点Z座標ではなくスライスメッシュのレイヤーインデックス（`lay`）単位に変更。
        - 同一レイヤー内のすべての頂点が一律に同一の変位量を共有するため、フラット段差の側面ポリゴンが**100%厳密に垂直（Z軸平行）**を維持し、テーパーによる傾斜・オーバーハング・自己交差を物理的に根絶。
    2. **天面・底面の境界保護（Pristine Rim Protection）**:
        - 天面キャップに接する最上2レイヤー（および底面1レイヤー）を完全に変位0.00mm（ベース円筒の真円）に固定。
        - これにより天面キャップ（Z=modelMaxZ）と側面が完全に同一半径でシームレスに接合され、最上面直下の不要なシェルフ三角形（棚面）を**502個から0個（完全消滅）**に排除。
    3. **実機スライス検証**:
        - φ55.3・実機テクスチャ・実機設定（フラット段差、凸0.30mm、凹0.00mm、グラデーション）の 3MF を PrusaSlicer でスライスし、Open Edges: 0、放射状櫛の歯インフィルの完全消滅、および φ80 安定版と同一のクリーンなスライスパス出力を確認。

---

## [v1.0.32] - 2026-09-18
### Fixed
- **ルーバー変位の極小下駄（マイクロ段差）根絶による階段状オープンエッジの完全解消 (Open Edges: 0)**:
  - φ55.3・実機テクスチャ等の急峻な白黒境界において、ルーバープロファイルの変位振幅 `effAmp` がレイヤー厚み `t`（0.20mm）をごく僅か（0.2 µm程度）超えた際に、ルーバー最下端に 0.2 µm の微小な底面段差（`base`）が発生していた問題を特定。
  - この微小段差によって生成された幅 0.2 µm の微小シェルフ三角形が、3MFエクスポート時の頂点統合（ウェルダー許容誤差 0.2 µm）で 1 辺が潰れて縮退三角形となり、縮退面削除フィルターで破棄された結果、相棒を失ったボディ側のエッジが PrusaSlicer で「階段状オープンエッジ（ピンク色の線）」として検出されていたメカニズムを解明。
  - `effAmp - t < 0.001mm`（1 µm未満）の極小下駄段差を完全に 0 にクランプ（スロープ傾きに吸収）し、微小変位（1 µm未満）も 0 に丸める処理を導入。
  - 155万ポリゴンの実機円筒モデルにおいて、**Open Edges: 0, Non-Manifold Edges: 0** の完全マニホールドを達成。

---

## [v1.0.31] - 2026-09-18
### Fixed
- **モジュールキャッシュバスターの伝播漏れ解消 & 境界フェード長を 1.2mm に最適化**:
  - `main.js` 内部で `layerSlicing.js?v=20260918_129` が固定指定されていたため、ブラウザ（Brave等）がキャッシュを保持し続け、v1.0.30 の境界フェード処理が適用されていなかった問題を修正。
  - φ55.3の円筒では、φ80に比べて円筒マッピングのV方向伸長率が高く、高コントラストテクスチャの急峻な白黒境界がちょうど天井付近（Z=99〜100mm）に交差しやすい幾何学的特性に対応。
  - 天面・底面のフェードアウト区間を約6層分（1.2mm）に拡充し、スライサーの微小オーバハング認識やインフィル誤認を確実に根絶。

---

## [v1.0.30] - 2026-09-18
### Fixed
- **天面・底面フチの櫛の歯インフィルおよび階段状オープンエッジの解消（Smoothstep 境界フェードアウト）**:
  - 斜めテクスチャ等で天面キャップ（Z=modelMaxZ）と最上層側面ルーバーの間に生じる微小なヒサシ段差により、スライサー（PrusaSlicer等）が全周に放射状の櫛の歯（ギャップフィル・インフィル）を生成し、直下のシェルフが階段状のオープンエッジ警告として検出されていた問題を解消。
  - 天面・底面の手前 4〜5層分（約0.8mm）において、ディスプレイスメント量およびシェルフ段差幅を三次滑らか関数（Smoothstep）で元の円筒形状に向かって滑らかに 0 に収束させるテーパー処理を実装。
  - 天面フタと側面の最上端が同一円周（真円）で隙間なく完全に面一結合し、スライサーの放射状櫛の歯とオープンエッジを完全根絶（Undirected Open = 0, Winding Mismatches = 0）。

---

## [v1.0.28] - 2026-09-18
### Fixed
- **水平段差面（シェルフ三角形）の巻き順（Winding Order）反転によるオープンエッジ・全層分解の根本修正**:
  - 層間の厚み段差を埋める水平シェルフ三角形の頂点順序が本体面と同方向（順方向）になっていたため、全層のエッジが結合せず 1000個以上のバラバラなシェルに分解され、476万個のオープンエッジが発生していた致命的バグを特定・修正。
  - シェルフのエッジ向きを下層・上層本体面と正しく対向する向き（逆方向）に反転し、ワインディング不整合を 57万件から 0件（完全解消）に修正。

---

## [v1.0.27] - 2026-09-18
### Fixed
- **上層シェルフY座標計算タイポ修正による巨大壁の飛び出し解消**:
  - `p0_top_y = p0[0] + ...` と誤って X座標を参照していた計算タイポを `p0[1]` に修正。Y=0面へ向かって巨大な壁が斜めに突き刺さり、形状が崩壊して大量のオープンエッジが出ていた不具合を解消。

---

## [v1.0.26] - 2026-09-18
### Fixed / Performance
- **交互積層3MFエクスポート時の 88% メモリ不足（細分化中にメモリが不足しました）エラー解消**:
  - 進捗88%（3MFモデル構造を構築中）において、3MF XMLの生成およびバッファ確保（`alloc`）でメモリ枯渇が発生していた問題を根本解決。
  - **直接ポリゴンクリップスライサー（Direct Polygon Clipper）への刷新**:
    - `sliceMeshWatertight` 内で従来の再帰二分木分割が、1個の三角形を数百〜数千個の極小三角形に爆発分割（420面が267万面に膨張）していた幾何学的バグを特定。
    - 切断平面群を順番にスキャンして各層ごとに台形（きっかり2三角形）を切り出す直列ポリゴンクリップ方式に全面刷新。スライス後の三角形数を 267万面から **8.4万面（1/32）** に劇的に圧縮し、XMLサイズおよびメモリ消費を大幅削減。
    - 開いたエッジ（Open edges = 0）および非多様体（Non-manifold = 0）を幾何学的に100%保証。
  - **細分化エッジ長の適正化**:
    - 交互積層モードにおける細分化エッジ長の下限を 2.5mm に設定。Z方向（0.20mm）のスライサー分解能と円周方向の高精細度を完璧に両立し、スライス後の総三角形数を快適な 100万面前後にコントロール。

---

## [v1.0.25] - 2026-09-17 ★（φ80 安定版）
### Fixed / Performance
- **交互積層3MFエクスポート時の `Invalid array length` エラー解消**:
  - 進捗78%（レイヤー整合テクスチャを変形適用中）において、V8（Brave/Chrome）の配列サイズ上限およびメモリ制限に抵触して発生していた `RangeError: Invalid array length` を根本修正。
  - **TypedArray チャンク化によるメモリ根絶**:
    - `sliceMeshWatertight` 内の出力三角形保持を、数百万個の JavaScript Array オブジェクト生成から固定長 TypedArray チャンク（`Int32Array`）方式へ全面移行。
    - `applyLayerAlignedDisplacement` の段差面（シェルフ三角形）生成において、通常の JavaScript 配列への数千万回に及ぶ `.push()` を撤廃し、TypedArray チャンク（`Float32Array` / `Int32Array`）へ直接書き込む方式に刷新。配列の多重リサイズとヒープ溢れを100%根絶。
  - **細分化解像度セーフガードの適正化**:
    - 交互積層モードにおける細分化エッジ長の下限を 1.5mm に適正化。Z方向は 0.20mm のスライサーで微細切断されるため、円周方向の美しい円筒形状（φ55.3で116角形以上、滑らかな外観）を完璧に保ちつつ、無駄な数百万面の過剰分割を抑止。

---

## [v1.0.24] - 2026-09-17
### Fixed / Performance
- **交互積層（振り重ね）3MFエクスポート時のメモリ不足（Out of Memory）クラッシュ解消**:
  - φ55.3など特定の円筒・メッシュで交互積層（振り重ね）モード＋カラー3MFエクスポートを実行した際、Brave/Chromeが `エラー コード: Out of Memory` でクラッシュする不具合を根本修正。
  - **円筒キャップ面（上面・底面）の自動除外**: 円筒マッピング時、テクスチャ変形を受けない平坦なキャップ面を細分化除外マスク（`faceWeights`）に自動登録し、不要な数十万〜百万ポリゴンの生成を阻止。
  - **交互積層モードにおける解像度セーフガード**: Z方向はスライサー（0.20mm）が自ら決定するため、ベース細分化エッジ長に安全な下限（`>= 1.0mm`）を設定し、数百万ポリゴンの過剰爆発を防止（円周170分割以上で真円度5ミクロン以下の超高精度を維持）。
  - **3MFエクスポート時のメモリ削減（BigIntビットパック）**: `exportMultiColor3MF` 内の重複面検出において、数百万個の文字列生成を BigInt 64bit 整数キーによるビットパックに置換し、ヒープ消費量を 1GB 以上削減。
  - **サンプラー内の Vector3 インスタンス再利用**: `sampleFn` 内での頂点ごとの `new THREE.Vector3` 生成を排除し、ガベージコレクションによるブラウザ一時フリーズを根絶。

---

## [v1.0.23] - 2026-09-14
### Added / Enhanced
- **高解像度カスタムテクスチャ対応 (4096px安全上限)**:
  - ユーザーがアップロードしたカスタムテクスチャについて、従来の長辺512pxへの強制縮小を撤廃し、最大4096pxまで**原寸のまま読み込み・保持**するようパイプラインを改良（φ80x100mmの円筒側面用 2513x1000 画像など、0.1mm/px級の高精細画像を忠実に再現可能に）。
  - エクスポート時（振り重ねスライシングおよびディスプレイスメント）は原寸解像度データから直接サンプリング。サンプリング計算量は $O(1)$ のため処理速度を一切落とさずに最高品質で出力。
- **テクスチャぼかし（Smooth）処理の安全クランプ**:
  - スムージング適用時の 3×3 タイリング処理において、元画像が2048pxを超える場合は一時Canvasを長辺2048px以下に自動クランプし、ブラウザのメモリ枯渇（Out of Memory）やタブクラッシュを完全に防止。
- **カラー量子化 & サンプリングの高速化・GC削減**:
  - `quantizeImage` および `getToolAtUV` 内の距離計算ループにおける一時配列アロケーションを排除し、数メガピクセル級の高解像度画像でも画面フリーズやGC停止なくスムーズに動作するよう最適化。
- **CHANGELOG.md の新設**:
  - 本更新履歴ドキュメントをプロジェクトルートに作成。

---

## [v1.0.22] - 2026-09-14
### Fixed
- **非正方形テクスチャのアスペクト比補正（振り重ねエクスポート）**:
  - 正方形以外のテクスチャ（横長・縦長画像）を使用した際、3Dプレビューでは正しく表示されるがマルチカラー3MFエクスポート時にアスペクト比が正方形（1:1）に歪んでしまう不具合を修正。`textureAspectU` / `textureAspectV` をエクスポートサンプラーへ正確に伝達。

---

## [v1.0.21] - 2026-09-14
### Changed
- **振り重ねモードのデフォルト階調表現を「グラデーション」に変更**:
  - 交互積層（振り重ね）タブ選択時の初期設定を「二値（くっきり境界）」から「グラデーション（連続階調）」に変更。写真や滑らかなパターンの混色表現が初期状態で美しく出力可能に。

---

## [v1.0.20] - 2026-09-13
### Added / Enhanced
- **グラデーション振り重ねモード時のプレビュー改善**:
  - グラデーションモード選択時、減色処理によるポスタリゼーション（色とび）をバイパスし、元画像の滑らかな階調プレビューをそのまま表示するよう最適化。
- **ドキュメント拡充**:
  - 実際の造形サンプル写真、推奨設定（ペリメータ2等）をREADMEに追加。

---

## [v1.0.19] - 2026-09-13
### Changed
- **プリセットモデル（ボウル）の形状再設計**:
  - FDM方式での安定出力を実現するため、オーバーハング角度を45度以下に抑えた形状へ刷新。

---

## [v1.0.18] - 2026-09-13
### Changed
- バージョン番号の統一および内部クリーンアップ。

---

## [v1.0.17] - 2026-09-12
### Added
- **新テクスチャプリセットの追加**:
  - 「Japanese Modern（和風モダン）」「Japanese Pattern（和風幾何学文様）」を追加。

---

## [v1.0.16] - 2026-09-12
### Added
- **カラー＆マルチツールUIの多言語対応**:
  - 全14言語でのローカライズ対応を完了。

---

## [v1.0.15] - 2026-09-12
### Changed
- **テクスチャプリセット名の英語統一**:
  - エクスポートファイル名が日本語による文字化けやスライサー側の不具合を起こさないよう、プリセットの内部英名をクリーン化。

---

## [v1.0.14] - 2026-09-12
### Fixed
- 多言語辞書ファイル内の構文エラー（カンマ漏れ）を修正。

---

## [v1.0.13] - 2026-09-11
### Enhanced
- エクスポート進捗バーのアニメーション平滑化。
- CNC Kitchen元プロジェクトへの謝辞・フォーク表示の整備。

---

## [v1.0.12] - 2026-09-11
### Fixed
- **Windows Explorer 3Dサムネイルの復活**:
  - 3MFファイル内の不要なマテリアル拡張記述を削除し、Windows Explorer上で3Dサムネイルが正しくプレビューされるよう修正。

---

## [v1.0.11] - 2026-09-11
### Fixed
- **1μm TolerantPointMap導入による完全閉多様体（0 open edges）の達成**:
  - PrusaSlicer / Bambu Studioでのスライス時に「非多様体エッジ」警告が出ない完全な水密（Watertight）メッシュ生成を実現。

---

## [v1.0.10] - 2026-09-10
### Fixed
- シェルフ（段差）のfloat32精度不一致に起因する292本のオープンエッジを修正。
- 3MF内のサムネイル画像をZIP先頭に配置し、スライサーのプレビュー読み込みを高速化。

---

## [v1.0.9] - 2026-09-10
### Fixed
- 単一段差テクスチャ境界におけるオープンエッジを非縮退三角シェルフの導入により解消。

---

## [v1.0.8] - 2026-09-10
### Fixed
- シェルフ面の頂点ワインディング順（表裏）を修正し、法線の反転によるピンク色ハイライト表示を解消。

---

## [v1.0.7] - 2026-09-10
### Fixed
- 水平キャップ面のアンテクスチャツール割り当てを正常化。

---

## [v1.0.6] - 2026-09-09
### Enhanced
- モデルロード時に最上面（Top planar surface）を自動的にテクスチャ除外対象に設定し、上面への不要な凹凸混入を防止。

---

## [v1.0.5] - 2026-09-09
### Fixed
- シェルフ境界での非多様体エッジ警告を解消。

---

## [v1.0.4] - 2026-09-09
### Fixed
- Bambu StudioおよびPrusaSlicerにおける3MFサムネイル表示仕様の整合。

---

## [v1.0.3] - 2026-09-09
### Enhanced
- `topAngleLimit`（上面角度制限）の初期値を5度に設定し、平面への不要な凹凸適用を自動抑制。

---

## [v1.0.2] - 2026-09-09
### Fixed
- 二重ディスプレイスメントのバグ修正。
- レイヤー境界の傾斜（Slant）を修正し、厳密な水平ステップに統一。
- 3MFサムネイル生成の追加。

---

## [v1.0.1] - 2026-09-08
### Fixed
- **モアレ縞・微小凹凸の解消**:
  - ベースメッシュを各レイヤー境界面で厳密に事前スライスしてからレイヤー変位を適用するパイプラインへ刷新し、規則的なガタつきを根本排除。

---

## [v1.0.0] - 2026-09-07
### Initial Release
- **BumpMesh_Color 初期リリース**:
  - CNC Kitchenの stlTexturizer をベースに、マルチカラー3MFエクスポート機能を追加。
  - カラー量子化（k-means）モードおよび交互積層（振り重ね）マルチツールスライシングエンジンの実装。
  - フラット段差プロファイル、スティッキーエクスポートフッターなどのUI刷新。
